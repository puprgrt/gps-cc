# Nixpacks configuration for Railway / container deployments
# Ensures all devDependencies and build tools are installed properly without omitting dev deps

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
